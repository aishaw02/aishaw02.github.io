# 个人网站项目交接

更新时间：2026-08-16（设备切换版）

当前设备项目目录：`/Users/jyxc-dz-0100481/Documents/pupu OS/personal-site`

GitHub 仓库：`https://github.com/aishaw02/aishaw02.github.io`

正式网站：`https://aishaw02.github.io/`

默认分支：`main`

当前已发布基线：`c9eabb5`（`Finalize portfolio content and mobile interactions`）

## 1. 项目目标

这是王方浦（Fangpu / Aisha Wang）的个人作品集网站，用于呈现个人经历、项目、思考与日常生活。

网站已经通过 GitHub Pages 正式上线。后续仍遵循“先本地修改和确认、用户明确说上线后再统一发布”的原则。

## 2. 设计方向

整体偏好来自 Le Labo、观夏，以及简洁的杂志 / 手帐排版：

- 关键词：线条、简洁、克制、编辑感、轻微手帐感、夏日清凉。
- 页面以大面积留白、细线、明确栅格和图片为主。
- 中文展示字体偏观夏式宋体气质；正文保持清晰易读。
- 英文使用窄体、略扁、有质感的字体。项目内已有 Vincendo 字体资源。
- 避免蓝色手绘线、星星、复杂涂鸦和过多装饰。
- 不使用强制滚动吸附或“一屏一页”的页面切换效果；允许用户停留在任意滚动位置。
- 各章节应保持独立，但章节间仅用克制的留白或细线过渡。
- 详情页顶部导航固定；当前栏目使用文字下划线表示，不使用色块或阴影。
- 图片不得额外压缩、降亮度、加灰雾滤镜或拉伸变形。
- 必须优先保留人物、人脸和图片中的文字；必要时使用 `object-fit: contain` 与白色补边，而不是裁切。
- 文案、标点、大小写和图片顺序均以用户提供内容为准，不要擅自改写。

## 3. 技术信息

- Next.js 16.2.6
- React 19.2.6
- Vinext 0.0.50
- 网站配置文件：`.openai/hosting.json`
- 本地默认地址：`http://localhost:3000/`
- GitHub Pages 工作流：`.github/workflows/deploy-pages.yml`
- GitHub Pages 构建命令：`npm run build:pages`
- GitHub Pages 构建产物：`out/`
- 推送到 `main` 后，GitHub Actions 自动构建并部署正式站点。

常用命令：

```bash
cd "/Users/jyxc-dz-0100481/Documents/pupu OS/personal-site"
npm run dev
```

其他命令：

```bash
npm run build
npm run lint
npm run start
```

如果 `localhost:3000` 无法访问，通常是开发服务器已经停止。重新执行 `npm run dev`，等待编译完成后再刷新浏览器。若页面没有体现最新修改，先硬刷新；仍无变化时重启开发服务器。

## 4. 重要开发原则

1. 交接时仓库已发布到 `c9eabb5`。新设备接手后必须重新执行 `git status`，以磁盘与 Git 的实时结果为准；任何未提交改动都视为有效用户资产，不要 `git reset --hard`、不要回滚、不要清理。
2. 修改前先查看现有组件和 CSS，延续当前实现，不要从旧对话重新推测或重建网站。
3. 使用 `apply_patch` 修改代码；避免无关的大规模重构。
4. 每次修改后至少检查对应路由；高风险改动再运行 build / lint。
5. 除非用户明确说“上线 / 发布”，不要 commit、push 或触发部署；用户只说“修改”时一律保留在本地。
6. 如果图片显示发灰，检查父元素的 `opacity`、伪元素遮罩、`filter`、动画初始态和叠加层，而不只是图片本身的 CSS。

## 5. 核心文件

- `app/globals.css`：全站样式与响应式规则。
- `app/layout.tsx`：全局 metadata、字体与页面结构。
- `app/page.tsx`：首页及四个导览章节。
- `app/HeroCarousel.tsx`：首页 Hero 轮播。
- `app/journey/page.tsx`：Journey 时间线详情页。
- `app/projects/page.tsx`：Projects 页面入口。
- `app/projects/ProjectsShowcase.tsx`：项目卡片、项目详情弹层和滴滴图片画廊。
- `app/projects/easter-egg/EasterEggBook.tsx`：第四篇项目彩蛋的真实书页内容、翻页状态与手势交互。
- `app/thoughts/page.tsx`：Thoughts 三篇真实长文、文章目录、详情弹层与文章卡片。
- `app/life/page.tsx`、`app/life/LifeFeed.tsx`：朋友圈式 Life 页面。
- `public/`：正式使用或候选使用的图片、简历及字体。
- `design-references/`：设计参考图，保留用于后续比对。

## 6. 路由与当前状态

| 路由 | 状态 | 说明 |
| --- | --- | --- |
| `/` | 已完成主体 | Hero 轮播、四个栏目导览、页脚均已实现 |
| `/journey` | 内容较完整 | 本科、研究生、学生工作、四段实习时间线 |
| `/projects` | 主体内容完成，部分画廊素材可继续补 | 四张项目卡片；四篇真实内容均已写入，滴滴与开放平台已有画廊 |
| `/thoughts` | 三篇真实文章已完成适配 | 三篇文章均已写入正文，使用统一长文版式、目录与文章信息侧栏 |
| `/life` | 四篇内容与交互已完成 | 四组真实九宫格、标题、正文、日期、详情与单图查看均已完成 |

## 7. 首页现状

顶部固定导航：

- FP（返回首页）
- JOURNEY
- PROJECTS
- THOUGHTS
- LIFE
- 右侧标签：`27届`、`北师大统计学硕`、`中共党员`
- `DOWNLOAD CV`

Hero：

- 主标题：`你好，我是王方浦`
- 英文：`Welcome to my world.`
- 入口：`EXPLORE`
- 自动轮播间隔：2 秒
- 支持左右按钮、键盘和触摸手势
- 鼠标悬停或聚焦时暂停
- 切换动效为左右滑动
- 当前轮播图：
  1. `hero-photo-water-sunset.jpg`
  2. `hero-slide-lake.jpg`
  3. `hero-slide-garden-collage.webp`（网站无损 WebP；原始 PNG 保存在 `assets-original/`）

四个导览章节：

1. 行迹 / `on the way`
   - 图片：`journey-campus.jpg`
   - 导语：`从校园到职场，一段关于成长的时间线。`
   - 正文：`记录了我学习经历、实习经历，以及每一次重要的选择与转折。每一段经历都有它存在的意义，也共同塑造了今天的我。`
   - CTA：`查看我的时间线` → `/journey`
2. 实践 / `in practice`
   - 图片：`home-practice.jpg`
   - 导语：`真实项目的完整记录，也是成长的过程。`
   - 正文：`收录了我参与过的校园项目、实习实践以及个人作品，不仅记录做了什么，也记录为什么这样做、最终取得了什么结果，以及我从中获得了哪些新的思考。`
   - CTA：`让我成长的项目` → `/projects`
3. 所思 / `in reflection`
   - 图片：`home-reflection.jpg`
   - 导语：`有些问题没有标准答案，但依旧值得反复思考`
   - 正文：`不是具体的项目复盘，也不是成长鸡汤，而是一路走来逐渐形成的认知：关于 AI、关于工作，以及关于如何认识自己。`
   - CTA：`从思考中探索答案` → `/thoughts`
4. 日常 / `beyond work`
   - 图片：`home-daily.jpg`
   - 导语：`工作之外，让我成为自己的日常。`
   - 正文：`旅行、摄影、音乐、Citywalk、美食……这里记录生活里的小事，也是我获取灵感、保持好奇心和感受世界的方式。`
   - CTA：`和我一起热爱生活` → `/life`

四个导览区首句均加粗。仅桌面端说明正文统一约 `16.5px`，CTA 统一约 `15px`；移动端保持原有阅读比例。

首页页脚：

- 大字：`未完待续…`
- 中部：`好奇 · 思考 · 探索 · 热爱`
- 邮箱：`aishawang02@163.com`
- 底部：`MADE WITH CHATGPT & CODEX`
- 中部文案需与底部制作信息保持视觉对齐。

## 8. Journey 详情页

页面为纵向时间线，顶部导航固定，JOURNEY 使用下划线表示当前栏目。

### 本科

- 兰州大学 · 经济学院
- 标签：985、双一流
- 经济学学士；GPA 4.07 / 5.0；专业排名 2 / 45
- 说明：`从理论基础出发，理解资源配置、消费者行为与市场运行逻辑，为认识用户需求、商业模式与产品价值奠定基础。`
- 学生工作：经济学院学生会 · 事务与权益部
- 职位：部长

### 研究生

- 北京师范大学 · 统计学院
- 标签：985、双一流
- 统计学学术型硕士；GPA 3.7 / 4.0
- 说明：`在理论基础上，以数据发现问题、验证假设，并在实践中不断理解真实的人、真实的业务与真实的选择。`
- 学生工作：统计学院就业工作室 · 活动部
- 职位：部长

### 实习

1. 中信证券北京分公司 · 呼家楼营业部
   - 2024.02—2024.03
   - 职位：市场拓展部 实习生
   - 已填详细内容
   - 不要添加能力标签
2. 滴滴出行 · 科技生态与发展部
   - 2025.07—2025.11
   - 技术运营 / 活动运营实习生
   - 标签：Hackathon、赛事运营、数据分析、跨团队协作
3. 商汤科技 · 大模型事业部
   - 2026.01—2026.03
   - 职位：产品运营 - 办公小浣熊
   - 已填 Agent 产品运营内容
   - 不要添加能力标签
4. 阶跃星辰
   - 2026.04—至今
   - 开放平台相关运营
   - 标签：AI开放平台、开发者增长、平台服务、海外适配

时间线中不要使用多余横隔线。章节分组依靠留白和时间轴表达；避免横线与纵轴相撞。

## 9. Projects 页面

页面标题：

- 小标题：`SELECTED WORK / 2025—2026`
- 主标题：`实践档案`

四篇卡片：

1. `📍 一场大赛｜滴滴 Hackathon` — `2025.7–2025.11`
2. `🧩 两条主线｜开放平台 × 开发者增长` — `2026.4–2026.8`
3. `⚙️ 三项活动｜校企联动 × 经验分享 × 生涯规划` — `2024.10–2026.2`
4. `🎬 第四面墙` — `2026.8–`

封面：

- `project-cover-01.jpg`
- `project-cover-02.png`
- `project-cover-03.png`
- `project-cover-04.png`

注意：第四篇标题中的“四”不要突出、不要额外留字距，正常大小展示。

点击卡片打开左图右文详情弹层。桌面端左右两栏均为 50%，画廊与正文等宽。左侧图片必须完整显示，以白底补充空白；禁止拉伸或裁剪。右侧标题只需比正文稍大、同字体、加粗，不再突出“一二三四”。

前三篇统一结构：

- 项目目标
- 我的工作
- 成果展示
- 工作心得
- 项目数据

“项目概览”已统一删除。

### 滴滴项目

滴滴项目已有真实内容。摘要必须保持为：

> 滴滴第四届黑客松 D- Hacks - 面向全公司举办，以真实业务问题为赛题，通过集中创新的方式连接业务创新、组织协作与人才发现。

当前画廊资源：

- `project-cover-01.jpg`（第一页）
- `didi-gallery-01.png`
- `didi-gallery-02.jpg`
- `didi-gallery-03.jpg`
- `didi-gallery-04.jpg`
- `didi-gallery-05.jpg`
- `didi-gallery-06.jpg`

支持画廊左右按钮和键盘方向键切换。第一页不能继续向前，最后一页不能继续向后，对应按钮禁用，禁止首尾循环。点击图片进入大图查看模式，初始以 `object-fit: contain` 完整显示原始比例，不拉伸、不裁切；单击图片或背景任意位置返回项目详情，双击不返回而是切换缩放。

大图模式（Projects 与 Life 共用）的现有交互：

- 双击在 100% / 200% 间切换。
- 鼠标滚轮、触控板和手机双指捏合缩放，范围 100%—400%。
- 放大后可拖拽查看局部。
- 提供 `－ / 当前倍率 / ＋ / 重置` 控件。
- 切换上一张 / 下一张或关闭大图时，自动恢复 100% 并居中。
- 左右箭头、键盘方向键、ESC、关闭按钮均可用。
- 实现结构为固定可视框 `.lifeZoomStage` 加绝对定位图片；不要再改回由图片固有尺寸直接参与 Grid 布局，否则高图会溢出后被 `overflow:hidden` 裁掉。

### 开放平台项目

标题：`两条主线｜开放平台 × 开发者增长`

已写入真实内容，包括：开放平台运营角色、Step 3.7 Flash 与 Step Plan 裂变增长、抖音店铺、海外适配、开发者活动、平台服务及个人观察。

“项目成果”当前内容为：

- 模型发布及裂变活动期间，平台新增注册用户数月环比增长 **300%+**。
- 参与模型服务在抖音渠道的上线与运营，产生 **1,500+ 订单**，核销率 30%+。
- 参与科技市集、开发者沙龙等 **3 场**线下活动，单场平均到场人数 **200+**。
- 调研 8 款国内 AI 开放平台的开发者服务现状，完成对比分析并产出专项报告。

网页中仅将 `300%+`、`1,500+ 订单`、`3 场`、`200+` 加粗，其余文字保持正常正文格式。

当前画廊资源按顺序为：

- `project-cover-02.png`（第一页）
- `platform-gallery-01.jpg`
- `platform-gallery-02.jpg`
- `platform-gallery-03.jpg`
- `platform-gallery-04.jpg`

交互与滴滴画廊一致。

### 校企联动项目

标题：`三项活动｜校企联动 × 经验分享 × 生涯规划`

已写入真实内容，包括学院就业工作室活动部部长角色、校企联动、经验分享、生涯规划、就业社群、项目成果及个人思考。详情中的小节标题为 `我做了什么`，不要恢复为带后缀的旧标题。

### 第四面墙项目

卡片预览标题保持为 `第四面墙`；详情弹层标题为：

> 打破第四面墙｜这个网站为什么是这样

已按桌面 `04.rtf` 的最新版本写入真实内容，包括项目出发点、产品定义、ChatGPT × Codex 协作实现、Vibe Coding 实践、持续迭代、参考文献与个人思考。

参考文献必须以可点击文字呈现，不在正文中显示完整 URL。当前包含观夏官网、小红书文章、LE LABO SANTAL 26 和微信朋友圈等参考说明。

正文末尾右侧有 `点击发现🎠 ↗`，跳转至 `/projects/easter-egg` 私人随笔 / 手记式彩蛋页。彩蛋页已经写入来自桌面 `彩蛋.rtf` 的最新完整内容并完成书页排版。现有内容在移动端共 7 页，最后一页为 `第一次更新`，8 月 15 日与 8 月 16 日上线日志合并在同一页；桌面端最后以单页加右侧空白书页呈现。该页字体、字号与其他次级正文一致，只略微收紧行距和段距。彩蛋页的 `BACK TO PROJECTS` 返回 `/projects?project=04`；Projects 页面会识别该参数并自动恢复第四篇详情弹层，而不是只回到项目列表。

移动端彩蛋已取消点击左右半页翻页，避免纵向滚动时误翻；仍保留左右滑动、底部 PREV / NEXT 和键盘方向键。桌面端仍支持点击左右半页翻页。

Projects 四篇详情正文已统一提高可读性：导语和正文适度放大、颜色加深、使用常规字重并略微收紧行距；窄窗口下同步使用适合长文阅读的字号。不要重新改回偏小、偏细、偏浅的旧样式。

## 10. Thoughts 页面

页面已调整为与 Projects 一致的编辑档案式标题和三列卡片布局，标题为 `思考手记`。三张卡片在常见桌面窗口中同排展示；移动端改为单列。

三篇文章详情使用统一的克制长文版式：顶部为紧凑标题区，正文与侧栏约为 `68 / 32` 分栏。桌面端右侧 `CONTENTS / ARTICLE INFO` 使用 sticky 固定，正文上下滚动时持续停留在侧边；移动端恢复普通文档流。目录支持点击跳转，页面自由滚动，不使用吸附。标题区已缩小至早期版本约一半，正文统一提高字重与对比度以保证阅读体验；`ARTICLE INFO` 下方不保留额外分割线。

当前三篇文章：

- `我与 AI`：副标题 `从旁观，到主动参与`；`1321 字 / 5 MIN READ`；正文共 7 节，目录第 05—07 项加粗。每节最后一小段按源文档使用不同字体呈现。
- `实习之后`：副标题 `关于负责，关于底气`；`849 字 / 4 MIN READ`；正文共 3 节。开头前言已改为与正文完全一致的字体、字号、字重和行高，不使用首字放大等特殊强调。
- `我这样看待自己`：副标题 `关于优缺点`；`913 字 / 4 MIN READ`。正文仅有 3 个主章节：`三个优点`、`由此带来的不足`、`总的来说`，目录也只能显示这 3 项。`结构感`、`同理心`、`责任感`及三个不足是章节内部小标题，不得再次拆成独立目录章节。保留源文档明确标注的 6 处加粗，全文正文使用统一字体，不套用第一篇的末段换字体规则。

当前封面依次为 `thought-cover-01-new.png`、`thought-cover-02.jpg`、`thought-cover-03-new.jpg`。卡片图片按既有样式呈现；文章详情侧栏在 `CONTENTS / ARTICLE INFO` 下方展示封面，支持点击查看。大图必须以原始比例完整显示，桌面端与移动端均支持缩放、拖拽和任意位置返回。三篇文章已分别根据桌面 `thoughts1`、`thoughts2.rtf`、`thoughts3.rtf` 的内容完成适配，不再使用占位正文。

## 11. Life 页面

视觉参考微信朋友圈：

- 用户提到 Life 的“第 1、2、3、4 组”时，默认始终按页面从上到下的当前显示顺序理解，不按代码中的历史组号或资源文件名判断。

- 昵称：IshA
- 个性签名：不要东张西望
- 头像：`life-avatar.jpg`
- 二维码：`life-qr.jpg`
- 二维码下方文案：`扫一扫，分享朋友圈`
- 顶部封面和置顶模块已删除，不要恢复。
- 每条动态可点击查看；关闭按钮、ESC 或点击背景可返回。

当前四篇均已加入真实九宫格、标题、正文和日期，页面显示顺序如下：

1. `摄影师修炼中 📸（进度10%）` — 8 月 12 日 — `life-group-01-01.jpg` 至 `life-group-01-09.jpg`
2. `音乐是生活的解药 🎸` — 8 月 15 日 — 代码内使用历史第三组资源 `life-group-03-01.jpg` 至 `life-group-03-09.jpg`
3. `早Coffee晚Alcohol ☕️🍹` — 8 月 15 日 — 代码内使用历史第四组资源 `life-group-04-01.jpg` 至 `life-group-04-09.jpg`
4. `生活花絮 📼` — 8 月 12 日 — 代码内使用历史第二组资源 `life-group-02-01.jpg` 至 `life-group-02-09.jpg`

四篇正文均已写入 `app/life/LifeFeed.tsx`，并保留用户指定的换行。第一篇正文首句固定为 `很喜欢用相机记录那些让我想停下来的瞬间`，不要恢复为此前带“一直”的版本。

九宫格列表和详情中的缩略图允许使用方形 `object-fit: cover`；但点击单张进入大图后，必须按原始比例完整显示，不能裁切或拉伸。

点击整组九宫格进入朋友圈式详情。统一布局标准：

- 头像为独立左栏。
- 桌面端详情保留弹层形式：头像为左栏，右侧依次为昵称、标题、正文和完整 3 × 3 九宫格。
- 移动端详情不是居中弹窗，而是占满手机视口的独立详情窗口；顶部为返回、`详情`、更多，内容顺序为头像 / 昵称、九宫格、标题、正文。
- 移动端昵称与九宫格之间保持紧凑，当前间距已缩短至此前约一半，不要再次拉大。
- 九张图片完整排成 3 × 3。桌面端九宫格受视口高度约束并保持靠左；移动端九宫格位于标题正文上方。
- 后续调整弹层时，不要把 `.lifeGroupDetailGrid` 恢复为单纯的 `width: 100%`，否则九宫格会重新变大并导致第三排落到首屏之外。
- 点击详情内任一缩略图进入单图大图模式。
- 单图缩放、拖拽、切换、返回规则与 Projects 完全一致。
- 四组各自在自己的 9 张图片内切换，不得串图；第一页和最后一页均禁止继续翻，不首尾循环。

列表预览规则：

- 宽桌面为日期在左、九宫格居中、标题与正文在右的左右结构；具体时间不显示。
- 宽桌面预览标题与正文均约 `16px`，保证可读性。
- 当窗口不足以让标题稳定保持单行时，自动切换为标题在上、九宫格在下的紧凑结构，并隐藏列表正文。
- 移动端日期与月份保持同一行，预览仅显示标题，不显示正文。

## 12. 主要资源

首页：

- `hero-photo-water-sunset.jpg`
- `hero-slide-lake.jpg`
- `hero-slide-garden-collage.webp`（网站无损 WebP；原始 PNG 保存在 `assets-original/`）
- `journey-campus.jpg`
- `home-practice.jpg`
- `home-reflection.jpg`
- `home-daily.jpg`

Journey：

- `journey-01.jpg` 至 `journey-08.jpg`
- `journey-campus.jpg`
- `journey-ginkgo.jpg`

Projects：

- `project-cover-01.jpg` 至 `project-cover-04.png`
- `didi-gallery-01.png`
- `didi-gallery-02.jpg` 至 `didi-gallery-06.jpg`
- `platform-gallery-01.jpg` 至 `platform-gallery-04.jpg`

Thoughts：

- `thought-cover-01-new.png`
- `thought-cover-02.jpg`
- `thought-cover-03-new.jpg`

Life：

- `life-avatar.jpg`
- `life-qr.jpg`
- `life-group-01-01.jpg` 至 `life-group-01-09.jpg`
- `life-group-02-01.jpg` 至 `life-group-02-09.jpg`
- `life-group-03-01.jpg` 至 `life-group-03-09.jpg`
- `life-group-04-01.jpg` 至 `life-group-04-09.jpg`

其他：

- 简历：`wang-fangpu-resume.pdf`
- 字体：`public/fonts/vincendo/Vincendo-Regular.ttf`

## 13. 待办与技术债

优先级建议：

1. 继续补充 Projects 第 3、4 篇后续画廊资源；第二篇成果数据与第四篇彩蛋正文已经完成，不再使用占位内容。
2. Thoughts 三篇真实摘要与正文已经完成；后续只根据用户反馈继续微调排版或内容。
3. Life 四篇真实九宫格、标题、正文和日期已经完成；后续仅根据用户反馈精修排版与内容。
4. 做 Mac 常见窗口宽度、移动端和超宽屏响应式检查。
5. 检查键盘焦点、图片 alt、弹层关闭、`prefers-reduced-motion`。
6. 替换或删除仍针对 Starter Project 的过时测试，再进行正式回归。
7. 完成后运行 lint / build 并检查浏览器控制台。
8. 用户明确提出发布后，再执行 GitHub Pages 发布流程；不要把 GitHub Pages 正式站点误发到其他托管域名。

音乐功能已经明确取消，不再加入网站，后续不要恢复或继续规划。

## 14. 2026-08-12 新增与确认

今天主要完成了以下内容：

1. Projects 第二篇加入开放平台真实文案和 5 张图片画廊，日期顺序修正为第二篇 `2026.4–2026.8`、第三篇 `2024.10–2026.2`。
2. Projects 画廊与 Life 单图查看统一增加缩放、捏合、拖拽、重置、键盘切换和任意位置返回。
3. 修复大图初始状态因图片固有尺寸溢出容器而被裁切的问题；现统一使用固定可视框和绝对定位图片，以 `contain` 完整显示原图比例。
4. Thoughts 改为与 Projects 一致的档案式 UI，标题为 `思考手记`；三篇标题、副标题和第一篇真实封面已经写入。
5. Journey 加入与 Projects 一致的标题区域，标题为 `成长行迹`，并收紧标题与内容之间的距离。
6. Life 第三、第四组各加入 9 张真实图片，按用户发送顺序保存；每组支持九宫格详情、单图查看及独立循环切换。
7. Life 九宫格详情统一为头像独立左栏、右侧昵称 / 文案 / 九宫格左对齐的朋友圈式结构；文案继续留空等待用户提供。
8. Life 九宫格详情进一步缩小：桌面端保持右侧内容区约 `88%` 宽度，并重新加入视口高度限制；头像、昵称与九宫格既有对齐方式不变，九张图优先在弹窗首屏内完整展示。

## 15. 2026-08-13 新增与确认

今天主要完成并确认了以下内容：

1. Projects 第三篇写入真实内容，标题为 `三项活动｜校企联动 × 经验分享 × 生涯规划`；详情包含校企联动、经验分享、生涯规划、就业社群、项目成果与个人思考，小节标题已统一为 `我做了什么`。
2. Projects 第四篇适配完整真实文案。卡片继续显示 `第四面墙`，详情标题最终确认为 `打破第四面墙｜这个网站为什么是这样`。
3. 第四篇参考文献改为点击文字跳转，完整链接不直接显示；保留观夏、小红书、LE LABO 与朋友圈等来源说明。
4. Projects 四篇详情桌面端统一为左右 `50% / 50%` 等宽分栏，左侧画廊与右侧正文宽度一致。
5. 修复 Projects 与 Life 在窄窗口、大图查看和上下结构下重复出现两套左右箭头的问题。根因是详情层箭头与大图层箭头同时可见；当前根据查看状态只保留一套有效导航，不要再次通过简单位置偏移掩盖问题。
6. Projects 与 Life 大图继续保持原始比例完整显示，并保留缩放、拖拽、重置、方向键和返回交互。
7. Projects 第四篇正文末尾新增右对齐入口 `点击发现🎠 ↗`，跳转至 `/projects/easter-egg` 的信笺样式页面；当前使用占位文案，等待补充网站制作彩蛋。
8. 信笺页 `BACK TO PROJECTS` 改为返回 `/projects?project=04`，Projects 页面读取参数后自动打开第四篇详情弹层。
9. Projects 四篇详情正文统一加强可读性：导语与正文略微放大、文字加深、采用常规字重、行距适度收紧；窄窗口同步调整。
10. 音乐功能已明确取消，不再加入网站。
11. 本轮所有修改均只在本地完成，未发布；构建检查通过。

## 16. 2026-08-14 新增与确认

今天主要完成并确认了 Projects 第四篇彩蛋页：

1. 彩蛋入口与返回路径
   - 第四篇详情正文末尾的 `点击发现🎠 ↗` 跳转至 `/projects/easter-egg`。
   - 彩蛋页 `BACK TO PROJECTS` 必须返回 `/projects?project=04`，恢复第四篇详情弹层，不能只返回 Projects 列表。

2. 内容与标题层级
   - 主标题固定为 `恭喜发现彩蛋🥳`。
   - 已删除 `混乱地记录一些心路历程（持续补充ing`，不要恢复。
   - 主标题下保留 `推荐阅读BGM：《珠玉》by 单依纯`；这只是阅读推荐，不是此前已经取消的网站音乐播放功能。
   - 后续章节依次为：`关于审美`、`关于网站的内容设置`、`关于克服完美主义`、`感谢我热爱拍照`、`还有新发现`。
   - 原文件中字体不同的重点语句在网页中使用加粗呈现；`关于审美`最后两段只加粗 `好的开始是成功的一半`，不要整段加粗。

3. 字体与正文排版
   - 第一页前言正文使用楷体字体栈：`Kaiti SC / STKaiti / KaiTi`；桌面端约 `15px`，行高约 `1.58`，段间距约 `9px`。
   - 后续章节正文不再使用宋体，改为更易读的无衬线字体栈：`PingFang SC / Hiragino Sans GB / Microsoft YaHei`。
   - 后续正文桌面端约 `14px`，移动端约 `15px`；保持常规字重。
   - 前言与所有后续正文统一使用 `text-indent: 2em`，即段落首行缩进 2 个字符。
   - 页码为黑色，放在每页内框右下角；前言字号、行距和段间距必须保证最后一段停在内框底线及页码上方，不能越线或遮挡页码。

4. 书本展示形式
   - 桌面端为真实双页展开，移动端退化为单页，但两端都保留 page-flip 感。
   - 桌面端书本整体控制在一屏内，无需上下滚动；当前外壳最大宽度约 `1040px`，书页高度最大约 `650px`，并受视口高度限制。
   - 保留纸张浅米色、细横线、内框、轻微纸张厚度、书脊阴影和克制的手帐质感，不要重新设计内容视觉。

5. 翻页实现与动效
   - 使用 React 状态与 CSS 3D 自行实现，没有引入第三方翻书依赖。
   - 翻动层由独立正面和背面组成，使用 `perspective`、`transform-style: preserve-3d`、`backface-visibility`、`rotateY`、动态渐变与投影模拟纸张从书脊翻动。
   - 下一页从右向左翻，上一页从左向右翻；翻动过程中正反面内容自然切换。
   - 自动翻页最终时长确认为约 `200ms`。不要恢复到此前尝试过的 820ms、620ms、300ms 或 100ms。
   - 尊重 `prefers-reduced-motion`；开启减少动态效果时直接切页，不执行 3D 动画。

6. 已确认的翻页方式
   - 鼠标单击书页左半区：上一页；单击右半区：下一页。
   - 底部 `PREV / NEXT` 按钮。
   - 键盘左右方向键。
   - 桌面端鼠标按住拖拽，移动端左右滑动；拖动距离不足时回弹。
   - 第一页不能继续向前翻，最后一页不能继续向后翻，对应按钮必须禁用。
   - 单击翻页必须由书本容器直接根据松开位置判断左右半区。此前拖拽通过 `setPointerCapture` 捕获事件，导致覆盖按钮收不到 `click`；不要重新改回只依赖左右透明按钮的实现。

7. 当前相关文件
   - 内容与交互：`app/projects/easter-egg/EasterEggBook.tsx`
   - 书页视觉、3D 翻页、字体与响应式：`app/globals.css` 中 `.easterEgg*` 规则。
   - 当前修改均为本地修改，未发布；最近一次构建检查通过。

8. 本日后续新增内容
   - 彩蛋最后一章已修正错别字：`万丈` → `网站`。
   - Projects 与 Life 图片切换均已取消首尾循环：到第一页或最后一页后不可继续翻，对应按钮禁用。
   - Thoughts 三篇文章均已从桌面源文档适配到网站，并统一为长文阅读版式；桌面目录与文章信息侧栏在滚动时保持固定。
   - 第一篇 `我与 AI` 当前为 `1321 字 / 5 MIN READ`，不再显示此前偏长的 `8 MIN READ`。
   - 第二篇 `实习之后` 当前为 `849 字 / 4 MIN READ`，前言与正常正文格式一致。
   - 第三篇 `我这样看待自己` 当前为 `913 字 / 4 MIN READ`；主章节与目录严格保持为 `三个优点 / 由此带来的不足 / 总的来说` 三项。
   - Projects 第二篇“项目成果”已补充注册增长、抖音订单、线下活动和平台调研四条真实结果，并按用户指定突出关键数字。
   - 当前所有修改仍仅在本地，未发布。

## 17. 2026-08-15 新增与确认

今天主要完成并确认了以下内容：

1. 网站 Home / Journey / Projects / Thoughts / Life 的主体内容已经初步构建完成，项目进入逐页检查、响应式校准与视觉精修阶段，不再处于搭建骨架或等待核心内容的阶段。
2. 首页四个导览区替换为用户提供的最终文案；每区第一句加粗，桌面端四区正文与 CTA 字号统一放大，移动端保持原有比例。
3. 首页移动端完成顶部导航、简历下载入口、Hero 图片比例、栏目图片居中和分区间距等适配；`/mobile-preview` 用于在桌面浏览器内查看完整手机端网站。
4. 顶部导航在页面滚动时固定；移动端删除身份标签，`DOWNLOAD CV` 使用文字加底部横线的克制形式。
5. Thoughts 三篇封面已更新；文章侧栏在目录与文章信息下方展示封面，支持点击查看、原比例完整显示、缩放与任意位置返回。
6. Life 四组真实九宫格已全部完成，并按页面当前顺序确定标题、正文和 8 月 12 日 / 8 月 15 日日期；以后提到第 1—4 组始终按当前页面从上到下的显示顺序解释。
7. Life 四篇详情内所有图片均支持单图查看、缩放、拖拽与边界限制。移动端详情改为独立全屏窗口，九宫格位于标题与正文之前；桌面端继续使用弹层。
8. Life 桌面列表恢复日期 / 九宫格 / 标题正文的左右结构，并提高标题正文可读性；空间不足时自动切换为只保留标题的上下结构。移动端详情中昵称与九宫格间距已进一步收紧。
9. Projects 和 Thoughts 已确认的大图、画廊和阅读交互保持不变；全站继续自由滚动，不加入滚动吸附。
10. 当前所有修改仍仅在本地，未发布；最近一次 `npm run build` 检查通过。

## 18. 历史记录使用说明

第 14—17 节记录了当时每天的阶段状态，其中“仅本地、未发布”等描述属于历史事实，不代表当前状态。设备切换与新 Agent 接手时，以文档顶部的仓库、正式网址、发布基线和下面第 19—24 节为准。

## 19. 新设备迁移清单

### 19.1 旧设备离开前

1. 确认网站代码已推送：

   ```bash
   cd "/Users/jyxc-dz-0100481/Documents/pupu OS/personal-site"
   git status
   git log -1 --oneline
   git remote -v
   ```

2. 如果 `git status` 不是干净状态，不要直接丢弃。先判断改动是否需要继续保留：
   - 需要随代码迁移：经过检查后 commit 并 push。
   - 暂时不准备上线但仍需保留：建立明确命名的临时分支并 push，或复制整个项目目录作为离线备份。
   - 不确定：保留原样并完整复制目录，不要执行 reset / clean。
3. 单独备份没有进入 Git 仓库的源文件，尤其是：
   - 桌面上的 `04.rtf`、`彩蛋.rtf`、`thoughts1`、`thoughts2.rtf`、`thoughts3.rtf` 等原始文稿。
   - 微信临时目录里的原图；微信 `RWTemp` 文件可能被系统清理，不能当作长期备份。
   - `assets-original/`、设计参考、简历源文件以及未来还可能替换的高分辨率图片。
   - 任何字体授权说明、域名 / GitHub 账号恢复信息。
4. 建议至少保留两份额外副本：一份云盘或 Time Machine，一份移动硬盘。备份完成后随机打开几张图片和文稿，确认文件可读，而不是只确认目录存在。

### 19.2 新设备准备

需要安装：

- Git
- Node.js 22 或更高版本（推荐 Node 22 LTS）
- npm
- GitHub CLI（可选，但便于授权与检查部署）
- Codex / 代码编辑器

验证：

```bash
git --version
node --version
npm --version
gh --version
```

GitHub 授权可使用：

```bash
gh auth login
gh auth status
```

不要把 GitHub Token、密码或其他凭证写入仓库、交接文档或 `.env.example`。

### 19.3 克隆与首次启动

在新设备选择一个稳定、会长期保留的本地目录，然后执行：

```bash
git clone https://github.com/aishaw02/aishaw02.github.io.git personal-site
cd personal-site
git switch main
git pull --ff-only origin main
npm ci
npm run dev
```

打开：`http://localhost:3000/`

新设备路径不必与旧设备相同。只需要把新路径同步写入本文件顶部，并在给新 Agent 的 Prompt 中使用真实绝对路径。

首次接手必须确认：

```bash
git status
git remote -v
git branch --show-current
git log -1 --oneline
```

预期结果：远端为 `aishaw02/aishaw02.github.io.git`、分支为 `main`，且不存在来源不明的本地改动。

## 20. 备份策略

### 20.1 GitHub 中已备份的内容

已提交并 push 的代码、`public/` 网站资源、构建配置和历史版本均保存在 GitHub。GitHub 是代码主备份，但不是所有原始素材的完整备份。

### 20.2 GitHub 之外必须备份的内容

- 未复制进项目的 RTF / Word 原稿。
- 微信、聊天软件或下载目录里的原始图片。
- 图片的未压缩原版、修图源文件和候选版本。
- 简历的可编辑源文件；仓库中的 PDF 只是网站下载版本。
- 尚未决定是否采用的设计参考。

### 20.3 大改前的安全点

大规模内容替换、响应式重构或上线前，可创建一个容易识别的 Git 标签：

```bash
git status
git tag backup-YYYYMMDD-description
git push origin backup-YYYYMMDD-description
```

只有在工作区干净、当前版本已经确认可用时才创建标签。标签用于找到安全版本，不代表可以随意覆盖当前未提交改动。

## 21. 日常修改标准流程

1. 进入仓库并同步：

   ```bash
   git status
   git pull --ff-only origin main
   ```

   如果已有未提交改动，先检查并保护它们，不要直接 pull、stash、reset 或覆盖。
2. 完整阅读本交接文档，再查看本次涉及的组件与 `app/globals.css`。
3. 启动 `npm run dev`，始终先在本地修改和预览。
4. 只改用户明确提出的范围；新文稿以用户最新提供的本地文件为内容源。
5. 明显错别字可以自动修正；涉及语义、经历数据、日期、成果数字或语气的改写必须谨慎，不能自行重写。
6. 图片先复制到项目的长期资源目录，再在代码中引用；不要直接引用桌面、微信临时目录或 `/var/folders/...`。
7. 修改后至少执行：

   ```bash
   npm run lint
   git diff --check
   ```

8. 上线前额外执行 GitHub Pages 的真实构建：

   ```bash
   npm run build:pages
   ```

9. 检查对应页面桌面端与移动端。高风险交互至少覆盖：弹层开关、返回、左右切图、首尾限制、缩放、纵向滚动、固定导航和简历下载。
10. 向用户说明修改内容与本地地址，等待确认。没有明确“上线”指令，不 commit、不 push。

## 22. Git 提交与 GitHub Pages 上线

正式上线只在用户明确说“上线 / 发布”后执行。

### 22.1 上线前

```bash
git status
git diff --check
npm run lint
npm run build:pages
```

确认：

- 正式网址、canonical、sitemap 和 robots 仍指向 `https://aishaw02.github.io/`。
- 简历下载文件名保持为 `王方浦-27届-北师大统计学硕.pdf`。
- 没有把桌面绝对路径、微信临时路径、Token、账号信息或无关文件带进提交。
- `git diff` 中只有本次确认要上线的改动。

### 22.2 提交与推送

只暂存本次要上线的文件，避免无脑 `git add .`：

```bash
git add <本次修改的文件>
git commit -m "简洁说明本次版本"
git push origin main
```

`main` push 后，`.github/workflows/deploy-pages.yml` 会自动运行 `npm ci`、`npm run build:pages` 并部署 `out/`。

### 22.3 检查部署

```bash
gh run list --repo aishaw02/aishaw02.github.io --limit 5
gh run watch <本次运行ID> --repo aishaw02/aishaw02.github.io --exit-status
```

部署成功后检查：

- `https://aishaw02.github.io/`
- `https://aishaw02.github.io/journey/`
- `https://aishaw02.github.io/projects/`
- `https://aishaw02.github.io/projects/easter-egg/`
- `https://aishaw02.github.io/thoughts/`
- `https://aishaw02.github.io/life/`

若线上仍显示旧内容，先等待 GitHub Pages CDN 更新，再强制刷新；不要因为短暂缓存重复 push 空提交。

## 23. 发布失败与恢复

1. GitHub Actions 构建失败：打开失败日志，先在本地复现 `npm ci && npm run build:pages`，修复后重新提交。
2. push 被拒绝：执行 `git fetch origin`，检查远端是否出现新提交；不要直接 force push。需要同步时优先使用 `git pull --ff-only`，无法快进则先人工比较。
3. 线上 404：确认仓库仍为公开、GitHub Pages Source 为 GitHub Actions、工作流成功、资源路径以 `/` 开头并适配根域名。
4. 必须恢复上一版时，优先用 `git revert <问题提交>` 创建可追踪的反向提交，再 push；不要使用 `git reset --hard` 或强制推送覆盖历史。
5. 正式站点异常但原因不明时，先保存当前工作区和日志，再处理；不要清理缓存目录以外的用户素材。

## 24. 可直接交给新设备新 Agent 的 Prompt

复制下面内容，并将尖括号里的路径替换为新设备上的真实绝对路径：

```text
请接手并继续维护我的个人网站。

新设备项目目录：
<新设备上的 personal-site 绝对路径>

GitHub 仓库：
https://github.com/aishaw02/aishaw02.github.io

正式网站：
https://aishaw02.github.io/

开始任何操作前，请先完整阅读：
<新设备上的 personal-site 绝对路径>/网站交接.md

然后依次检查：
1. git status、git remote -v、当前分支和最近一次提交；
2. package.json、.github/workflows/deploy-pages.yml、app/layout.tsx；
3. 本次涉及的核心页面组件和 app/globals.css；
4. public/ 中现有图片、简历和字体资源。

以新设备磁盘中的现有代码和 Git 状态为唯一准确信息源，不要根据旧对话重新搭建。不要回滚、清理、覆盖或擅自 stash 任何未提交改动；如果发现工作区不干净，先说明具体文件和状态。

固定工作原则：
1. 先执行 npm ci（仅首次或依赖变化时），再启动 npm run dev，并在 http://localhost:3000 本地预览。
2. 除非我明确说“上线”或“发布”，否则只做本地修改，不 commit、不 push、不触发部署。
3. 延续当前 Le Labo × 观夏 × 编辑杂志感的克制风格，保留自由滚动，不加入滚动吸附或强制翻页。
4. 保留现有文案、数字、日期、图片顺序、图片原比例和已确认交互；不要擅自压缩、加滤镜、裁脸或重写内容。明显错别字可以修正，但涉及语义的调整先谨慎核对。
5. 新图片和文稿必须先复制到项目长期目录，不要直接引用桌面、下载目录、微信 RWTemp 或 /var/folders 临时路径。
6. 修改后检查对应桌面端和移动端页面，运行 npm run lint 与 git diff --check；准备上线时再运行 npm run build:pages。
7. 简要告诉我改了什么、检查结果和本地查看地址。
8. 我明确要求上线后，才选择性 git add、commit、push origin main；等待 GitHub Pages Action 成功，并核对 https://aishaw02.github.io/。
9. 不要部署到 chatgpt.site 或其他新域名，除非我明确改变托管方案。

如果本地服务未启动，请启动开发服务器。完成上述接手检查后，先告诉我当前仓库是否干净、当前版本、可访问的本地地址以及你已阅读交接文档；不要先做未被要求的新设计。
```
